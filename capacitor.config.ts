import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ingholm.testinganalyzer',
  appName: 'RC Testing Analyzer',
  webDir: 'build'
};

export default config;
